const { Observer } = require("../utils");

class DialogueObserver extends Observer {
    constructor(scene){
        super();
        this.scene = scene;
    }

    update(observable, object){
        
    }
}